import styles from '../styles/404.module.css'
import Link from 'next/link'
import Head from 'next/head'
import { useRouter } from 'next/router'
import { useEffect } from 'react'

const ErrorPage = () => {
    const router = useRouter();
    const handleInput = () => {
        router.push("/");
    }

    useEffect(() => {
        setTimeout(() => {
            router.push("/");
        }, 5000)
    }, [])
    
    return (
        <>
            <Head>
                <title>404 Page</title>
                <meta name="description" content="Generated by create next app" />
            </Head>
            <div className={styles.wrapper}>
                <div className={styles.main_text} title="404">404</div>
                <p>Page Not Found</p>
                <div className={styles.btn_group}>
                    <Link href="/" legacyBehavior>
                        <a className={styles.btn}>Homepage in anchor tag</a>
                    </Link>
                    <button className={styles.btn} onClick={() => router.push("/")}>Homepage in button tag</button>
                    <button className={styles.btn} onClick={handleInput}>Homepage in button tag</button>
                </div>
            </div>
        </>
    )
}

export default ErrorPage;